<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.netease.edu.boot</groupId>
    <artifactId>ccyctrl-autoconfiguration</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>ccyctrl-autoconfiguration</name>
    <url>http://maven.apache.org</url>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <sonar-scan-execution-phase>site</sonar-scan-execution-phase>
        <jacoco.output>file</jacoco.output>
        <sonar-project-env-suffix-branch>scratch</sonar-project-env-suffix-branch>
        <sonar-project-env-suffix-id>scratch</sonar-project-env-suffix-id>
        <argLine>-XX:MaxPermSize=256M -Xmx1024m -Dfile.encoding=UTF-8 -da</argLine>
        <sonar-project-artifactId>${project.artifactId}</sonar-project-artifactId>
        <jacocoCoverageDir>**</jacocoCoverageDir>
        <edu-second-party-base.version>1.0.40.s2-micro-special-1-jt-20171113-SNAPSHOT</edu-second-party-base.version>
        <edu-third-party-base.version>1.0.3</edu-third-party-base.version>
    </properties>

    <dependencyManagement>
        <dependencies>

            <dependency>
                <groupId>com.netease.edu</groupId>
                <artifactId>edu-second-party-base</artifactId>
                <version>${edu-second-party-base.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>com.netease.edu</groupId>
                <artifactId>edu-third-party-base</artifactId>
                <version>${edu-third-party-base.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-starter-parent</artifactId>
                <version>Camden.SR7</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <!-- Import dependency management from Spring Boot -->
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>1.4.5.RELEASE</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>com.netease.edu</groupId>
            <artifactId>edu-base-util</artifactId>
        </dependency>
        <dependency>
            <groupId>com.netease.edu</groupId>
            <artifactId>edu-base-aop</artifactId>
        </dependency>
    </dependencies>

    <distributionManagement>
        <repository>
            <id>repo</id>
            <name>repo</name>
            <url>http://mvn.hz.netease.com/artifactory/libs-releases</url>
        </repository>
        <snapshotRepository>
            <id>snapshots</id>
            <name>snapshots-only</name>
            <url>http://mvn.hz.netease.com/artifactory/libs-snapshots</url>
        </snapshotRepository>
    </distributionManagement>
</project>
